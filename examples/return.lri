extern fn print(x: i64);

fn main() -> i64 {
    print(foo());

    if false {
        return 1;
    } else if true {
        return 2;
    }

    0
}

fn foo() -> i64 {
    return return return return return!!!!!!!!!!111
}

fn bar() -> f64 {
    let x: f64 = return 3.0;
    // The following should cause a type error
    // let y: f64 = false;
    // "no!"
}
