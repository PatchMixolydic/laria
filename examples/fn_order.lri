extern fn print(s: string);

fn f() {
    print("f!");
}

fn g(x: i64, y: i64, z: i64) {
    if z == z {
        print("ok!");
    } else {
        print("oh no!");
    }
}

fn main() {
    print("main!");
    f();
    g(1, 2, 3);
}
